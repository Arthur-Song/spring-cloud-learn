server:
  port: 8080
spring:
  application:
    name: @artifactId@
eureka:
  instance:
    hostname: localhost
  client:
    instanceInfoReplicationIntervalSeconds: 10
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/