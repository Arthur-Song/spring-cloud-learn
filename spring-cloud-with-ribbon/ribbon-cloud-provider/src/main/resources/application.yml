spring:
  application:
    name: @artifactId@
eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      default-zone: http://localhost:8761/eureka/

---
server:
  port: 8080
spring:
  profiles: server-1

---
server:
  port: 8081
spring:
  profiles: server-2